// src/components/DeviceDetails/DeviceConfig.js
import React from 'react';

const DeviceConfig = () => {
    return (
        <div>
            <h2>Device Configuration</h2>
            {/* Add device config details here */}
        </div>
    );
};

export default DeviceConfig;
