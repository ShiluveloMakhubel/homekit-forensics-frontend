import React from 'react';
import './SystemStatus.css'; // Import the CSS file

const SystemStatus = () => {
    return (
        <div className="card system-status">
            <h2>System Status</h2>
            {/* Add system status content here */}
        </div>
    );
};

export default SystemStatus;